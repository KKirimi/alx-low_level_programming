#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always (Success)
 */

int main(void)

{
	char c;

	for (c = '0'; c <= '9'; c++)
	{
		putchar(c);
	}

	for (c = 'a'; c <= 'f';)
	{
		putchar(c);
	}
		putchar(c);
		return (0);
}

#include "main.h"
#include "string.h"

/**
 * _print_rev_recursion - prints a string in reverse
 * @s: a string
 *
 * Return: a string in reverse
 */

void _print_rev_recursion
{
	char *s[5];

	printf("\n");
	gets(*s);

	strrev(s);

	printf("%s \n", *s);

	return 0;
}
